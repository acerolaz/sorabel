# frontend

@../CLAUDE.md

## Contexte

Interface(s) cliente(s) et administration de la gateway Sorabel. Stack non encore
déterminée — voir `README.md` pour le rôle attendu.

## Point d'attention

Pas de `.claude/` local ni de règles d'architecture tant que la stack n'est pas choisie
(pas de règles à inventer prématurément). Pas de Makefile tant que l'outillage
(npm/yarn/pnpm...) n'est pas fixé.
