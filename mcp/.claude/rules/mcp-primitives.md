# Primitives MCP — catalogue et matrice d'accès

Catalogue des tools, matrice d'accès (profil × tool × collections/tables).

> TODO : détailler le format de la matrice d'accès (YAML/JSON), la convention
> `@mcp.tool()` (FastMCP), et le filtrage `list_tools`/`call_tool` — cf. `README.md` §3-4
> pour la vue d'ensemble à reprendre ici.
