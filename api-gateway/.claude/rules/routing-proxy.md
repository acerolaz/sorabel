# Routage/proxy — api-gateway

Conventions de routage pur — **pas de logique RBAC ici** (cf. `mcp/`).

> TODO : détailler les conventions de mapping route → backend (`mcp`, `text2sql-ai`,
> `sorabelsql-api`), la gestion du relais du Bearer JWT, et les timeouts/retries.
